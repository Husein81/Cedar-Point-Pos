// Locale used for all date/number formatting (Intl.NumberFormat,
// toLocaleDateString, toLocaleTimeString, ...) so formatting stays
// consistent across the app.
export const DEFAULT_LOCALE = "en-US";
